import React from 'react';
import Button from '../Button';

const UserMenu = () => {
  return (
    <div>
      <p>Welcome, user</p>
      <Button title="Logout" />
    </div>
  );
};

export default UserMenu;
